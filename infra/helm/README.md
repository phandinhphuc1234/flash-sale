# Helm Infrastructure

This directory is reserved for approved Helm charts or values used to deploy shared or third-party
platform components. A feature plan must identify whether Helm or the resources under `infra/k8s/`
are the source of truth for each deployed resource; the same resource must not be maintained twice.

No Helm chart or release configuration is implemented in the current skeleton.

