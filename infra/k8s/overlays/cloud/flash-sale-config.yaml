apiVersion: v1
kind: ConfigMap
metadata:
  name: flash-sale-runtime-config
data:
  # Non-sensitive values shared by the application Deployments.
  SPRING_PROFILES_ACTIVE: kubernetes
  SERVER_PORT: "8080"
  JAVA_TOOL_OPTIONS: -XX:MaxRAMPercentage=75.0
