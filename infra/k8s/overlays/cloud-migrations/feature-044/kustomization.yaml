apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: flash-sale

# This overlay contains only the two Feature 044 service-owned migrations. The release-gate script
# replaces the placeholder images with verified immutable ECR release tags in a temporary manifest.
resources:
  - order-purchase-saga-migration-job.yaml
  - flash-sale-reservation-finalization-migration-job.yaml
