Generate tasks as vertical, independently verifiable slices. Preserve the core format
`TNNN [P?] [USN] Description`; add UC/AC/NFR references without replacing `[USN]`.

Include exact paths, dependency ordering, contract/migration/recovery work, risk-based tests,
validation commands, and evidence checkpoints. Test-first ordering is mandatory only where the
Approved plan selects TDD or contract-first delivery.

{CORE_TEMPLATE}

Before completion, reject vague tasks, orphan acceptance behavior, unsafe parallel markers, and
checkboxes that imply evidence before a validation step exists. Human reviewers own task approval.

