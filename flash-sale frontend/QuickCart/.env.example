# Local Docker Gateway used by the repository smoke environment.
NEXT_PUBLIC_API_BASE_URL=http://localhost:18080

# Optional: use this only when the Flash Sale campaign ID is known.
NEXT_PUBLIC_DEFAULT_CAMPAIGN_ID=

# Display-only currency label.
NEXT_PUBLIC_CURRENCY=₫
