package com.philia.flashsale.campaign.campaign.adapter.in.web.admin.request;

/** Empty body kept explicit so the schedule command remains extensible without changing headers. */
public record ScheduleCampaignRequest() {
}
