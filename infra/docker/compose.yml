name: ${COMPOSE_PROJECT_NAME:-flash-sale}

x-platform-health-defaults: &platform-health-defaults
  interval: 10s
  timeout: 5s
  retries: 10
  start_period: 20s

x-app-common: &app-common
  profiles:
    - apps
  networks:
    - flash-sale-net
  restart: unless-stopped
  expose:
    - "8080"
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_healthy
    kafka:
      condition: service_healthy

x-backend-environment: &backend-environment
  SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-docker}
  SERVER_PORT: "8080"
  JAVA_TOOL_OPTIONS: ${JAVA_TOOL_OPTIONS:--XX:MaxRAMPercentage=75.0}
  # Local-only opt-in. Cloud ConfigMaps deliberately do not set this value.
  API_DOCS_ENABLED: ${API_DOCS_ENABLED:-false}
  SPRING_THREADS_VIRTUAL_ENABLED: "true"
  SPRING_MAIN_KEEP_ALIVE: "true"
  SPRING_LIQUIBASE_ENABLED: "false"

x-backend-common: &backend-common
  <<: *app-common
  environment:
    <<: *backend-environment

x-flashsale-environment: &flashsale-environment
  <<: *backend-environment
  SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/flashsale_db
  SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-flashsale}
  SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
  SPRING_DATA_REDIS_HOST: redis
  SPRING_DATA_REDIS_PORT: "6379"
  SPRING_DATA_REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD must be set}
  SPRING_KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:-kafka:9092}
  SCHEMA_REGISTRY_URL: ${SCHEMA_REGISTRY_URL:-http://schema-registry:8081}
  KAFKA_AUTO_REGISTER_SCHEMAS: ${KAFKA_AUTO_REGISTER_SCHEMAS:-false}
  JWT_ISSUER_URI: ${JWT_ISSUER_URI:-http://authentication-service:8080}
  JWT_JWK_SET_URI: ${JWT_JWK_SET_URI:-http://authentication-service:8080/.well-known/jwks.json}
  FLASHSALE_JWT_ISSUER: ${FLASHSALE_JWT_ISSUER:-http://authentication-service:8080}
  FLASHSALE_JWT_AUDIENCE: ${FLASHSALE_JWT_AUDIENCE:-flash-sale-api}
  FLASHSALE_JWT_JWK_SET_URI: ${FLASHSALE_JWT_JWK_SET_URI:-http://authentication-service:8080/.well-known/jwks.json}
  OAUTH_TOKEN_URI: ${OAUTH_TOKEN_URI:-http://authentication-service:8080/oauth2/token}
  FLASHSALE_CLIENT_ID: ${FLASHSALE_CLIENT_ID:-flashsale-service}
  FLASHSALE_CLIENT_SECRET: ${FLASHSALE_CLIENT_SECRET:?FLASHSALE_CLIENT_SECRET must be set}
  FLASHSALE_INTERNAL_JWT_AUDIENCE: ${FLASHSALE_INTERNAL_JWT_AUDIENCE:-flash-sale-internal-api}
  CAMPAIGN_SERVICE_URL: ${CAMPAIGN_SERVICE_URL:-http://campaign-service:8080}
  FLASHSALE_PURCHASE_COMMANDS_TOPIC: ${FLASHSALE_PURCHASE_COMMANDS_TOPIC:-flashsale.purchase.commands.v1}
  FLASHSALE_RESERVATION_COMMANDS_CONSUMER_GROUP: ${FLASHSALE_RESERVATION_COMMANDS_CONSUMER_GROUP:-flashsale-reservation-commands-v1}
  FLASHSALE_RESERVATION_COMMANDS_DLT_TOPIC: ${FLASHSALE_RESERVATION_COMMANDS_DLT_TOPIC:-flashsale.flash-sale.purchase-command.dlt.v1}
  FLASHSALE_RESERVATION_COMMANDS_RETRY_DELAYS: ${FLASHSALE_RESERVATION_COMMANDS_RETRY_DELAYS:-500ms,2s}
  FLASHSALE_RESERVATION_COMMANDS_ENABLED: ${FLASHSALE_RESERVATION_COMMANDS_ENABLED:-true}
  FLASHSALE_RUNTIME_ENABLED: ${FLASHSALE_RUNTIME_ENABLED:-true}
  FLASHSALE_RUNTIME_RECONCILIATION_ENABLED: ${FLASHSALE_RUNTIME_RECONCILIATION_ENABLED:-true}
  FLASHSALE_RUNTIME_RECONCILIATION_INTERVAL_MS: ${FLASHSALE_RUNTIME_RECONCILIATION_INTERVAL_MS:-2000}

x-order-environment: &order-environment
  <<: *backend-environment
  SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/order_db
  SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-flashsale}
  SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
  SPRING_KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:-kafka:9092}
  SCHEMA_REGISTRY_URL: ${SCHEMA_REGISTRY_URL:-http://schema-registry:8081}
  KAFKA_AUTO_REGISTER_SCHEMAS: ${KAFKA_AUTO_REGISTER_SCHEMAS:-false}
  ORDER_JWT_ISSUER: ${JWT_ISSUER_URI:-http://authentication-service:8080}
  ORDER_JWT_JWK_SET_URI: ${JWT_JWK_SET_URI:-http://authentication-service:8080/.well-known/jwks.json}
  ORDER_JWT_AUDIENCE: ${JWT_AUDIENCE:-flash-sale-api}
  ORDER_JWT_TYPE: ${ORDER_JWT_TYPE:-at+jwt}
  ORDER_PURCHASE_ACCEPTED_TOPIC: ${ORDER_PURCHASE_ACCEPTED_TOPIC:-flashsale.purchase.events.v1}
  ORDER_PURCHASE_ACCEPTED_CONSUMER_GROUP: ${ORDER_PURCHASE_ACCEPTED_CONSUMER_GROUP:-order-purchase-accepted-v1}
  ORDER_PURCHASE_ACCEPTED_DLT_TOPIC: ${ORDER_PURCHASE_ACCEPTED_DLT_TOPIC:-flashsale.order.purchase-accepted.dlt.v1}
  ORDER_EVENTS_TOPIC: ${ORDER_EVENTS_TOPIC:-flashsale.order.events.v1}
  ORDER_PAYMENT_COMMANDS_TOPIC: ${ORDER_PAYMENT_COMMANDS_TOPIC:-flashsale.payment.commands.v1}
  ORDER_PAYMENT_EVENTS_TOPIC: ${ORDER_PAYMENT_EVENTS_TOPIC:-flashsale.payment.events.v1}
  ORDER_PAYMENT_EVENTS_CONSUMER_GROUP: ${ORDER_PAYMENT_EVENTS_CONSUMER_GROUP:-order-payment-events-v1}
  ORDER_PAYMENT_EVENTS_DLT_TOPIC: ${ORDER_PAYMENT_EVENTS_DLT_TOPIC:-flashsale.order.payment-result.dlt.v1}
  ORDER_PURCHASE_COMMANDS_TOPIC: ${ORDER_PURCHASE_COMMANDS_TOPIC:-flashsale.purchase.commands.v1}
  ORDER_PURCHASE_RESERVATION_RESULTS_TOPIC: ${ORDER_PURCHASE_RESERVATION_RESULTS_TOPIC:-flashsale.purchase.events.v1}
  ORDER_PURCHASE_RESERVATION_RESULTS_CONSUMER_GROUP: ${ORDER_PURCHASE_RESERVATION_RESULTS_CONSUMER_GROUP:-order-purchase-reservation-results-v1}
  ORDER_PURCHASE_RESERVATION_RESULTS_DLT_TOPIC: ${ORDER_PURCHASE_RESERVATION_RESULTS_DLT_TOPIC:-flashsale.order.purchase-reservation-result.dlt.v1}
  ORDER_ACCEPTED_PURCHASE_CONSUMER_ENABLED: ${ORDER_ACCEPTED_PURCHASE_CONSUMER_ENABLED:-true}
  ORDER_PAYMENT_EVENTS_CONSUMER_ENABLED: ${ORDER_PAYMENT_EVENTS_CONSUMER_ENABLED:-true}
  ORDER_PURCHASE_RESERVATION_RESULTS_CONSUMER_ENABLED: ${ORDER_PURCHASE_RESERVATION_RESULTS_CONSUMER_ENABLED:-true}
  ORDER_OUTBOX_PUBLISHER_ENABLED: ${ORDER_OUTBOX_PUBLISHER_ENABLED:-true}
  ORDER_OUTBOX_ENABLED: ${ORDER_OUTBOX_ENABLED:-true}
  ORDER_CREATION_ENABLED: ${ORDER_CREATION_ENABLED:-true}
  ORDER_QUERY_ENABLED: ${ORDER_QUERY_ENABLED:-true}

x-payment-environment: &payment-environment
  <<: *backend-environment
  # Keep Payment DNS entries fresh without disabling JVM caching globally for other services.
  JAVA_TOOL_OPTIONS: ${PAYMENT_JAVA_TOOL_OPTIONS:--XX:MaxRAMPercentage=75.0 -Dnetworkaddress.cache.ttl=60}
  SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/payment_db
  SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-flashsale}
  SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
  SPRING_LIQUIBASE_ENABLED: ${PAYMENT_LIQUIBASE_ENABLED:-false}
  SPRING_KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:-kafka:9092}
  SCHEMA_REGISTRY_URL: ${SCHEMA_REGISTRY_URL:-http://schema-registry:8081}
  KAFKA_AUTO_REGISTER_SCHEMAS: ${KAFKA_AUTO_REGISTER_SCHEMAS:-false}
  PAYMENT_PROVIDER: ${PAYMENT_PROVIDER:-STRIPE}
  PAYMENT_ACCEPTANCE_ENABLED: ${PAYMENT_ACCEPTANCE_ENABLED:-false}
  PAYMENT_CHECKOUT_ENABLED: ${PAYMENT_CHECKOUT_ENABLED:-false}
  PAYMENT_PUBLIC_API_PREFIX: ${PAYMENT_PUBLIC_API_PREFIX:-/api/v1/payments}
  PAYMENT_WEBHOOK_PATH: ${PAYMENT_WEBHOOK_PATH:-/webhooks/v1/payments/stripe}
  PAYMENT_JWT_ISSUER: ${PAYMENT_JWT_ISSUER:-http://authentication-service:8080}
  PAYMENT_JWT_JWK_SET_URI: ${PAYMENT_JWT_JWK_SET_URI:-http://authentication-service:8080/.well-known/jwks.json}
  PAYMENT_JWT_AUDIENCE: ${PAYMENT_JWT_AUDIENCE:-flash-sale-api}
  PAYMENT_JWT_TYPE: ${PAYMENT_JWT_TYPE:-at+jwt}
  PAYMENT_DNS_CACHE_TTL: ${PAYMENT_DNS_CACHE_TTL:-60s}
  STRIPE_ENABLED: ${STRIPE_ENABLED:-false}
  STRIPE_MODE: ${STRIPE_MODE:-test}
  STRIPE_API_BASE_URL: ${STRIPE_API_BASE_URL:-https://api.stripe.com}
  STRIPE_API_VERSION: ${STRIPE_API_VERSION:-2026-07-29.dahlia}
  STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:-}
  STRIPE_PUBLISHABLE_KEY: ${STRIPE_PUBLISHABLE_KEY:-}
  STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET:-}
  STRIPE_WEBHOOK_TOLERANCE: ${STRIPE_WEBHOOK_TOLERANCE:-5m}
  STRIPE_CONNECT_TIMEOUT: ${STRIPE_CONNECT_TIMEOUT:-5s}
  STRIPE_READ_TIMEOUT: ${STRIPE_READ_TIMEOUT:-20s}
  STRIPE_MAX_NETWORK_RETRIES: ${STRIPE_MAX_NETWORK_RETRIES:-2}
  PAYMENT_CHECKOUT_SUCCESS_URL: ${PAYMENT_CHECKOUT_SUCCESS_URL:-http://localhost:3000/payments/success}
  PAYMENT_CHECKOUT_CANCEL_URL: ${PAYMENT_CHECKOUT_CANCEL_URL:-http://localhost:3000/payments/cancel}
  PAYMENT_WEBHOOK_PROCESSING_ENABLED: ${PAYMENT_WEBHOOK_PROCESSING_ENABLED:-false}
  PAYMENT_WEBHOOK_PROCESSING_POLL_INTERVAL: ${PAYMENT_WEBHOOK_PROCESSING_POLL_INTERVAL:-1s}
  PAYMENT_WEBHOOK_PROCESSING_BATCH_SIZE: ${PAYMENT_WEBHOOK_PROCESSING_BATCH_SIZE:-100}
  PAYMENT_WEBHOOK_PROCESSING_CLAIM_LEASE: ${PAYMENT_WEBHOOK_PROCESSING_CLAIM_LEASE:-30s}
  PAYMENT_WEBHOOK_PROCESSING_MAX_ATTEMPTS: ${PAYMENT_WEBHOOK_PROCESSING_MAX_ATTEMPTS:-10}
  PAYMENT_WEBHOOK_PROCESSING_RETRY_BACKOFF: ${PAYMENT_WEBHOOK_PROCESSING_RETRY_BACKOFF:-10s}
  PAYMENT_REQUESTED_TOPIC: ${PAYMENT_REQUESTED_TOPIC:-flashsale.payment.commands.v1}
  PAYMENT_EVENTS_TOPIC: ${PAYMENT_EVENTS_TOPIC:-flashsale.payment.events.v1}
  PAYMENT_REQUESTED_DLT_TOPIC: ${PAYMENT_REQUESTED_DLT_TOPIC:-flashsale.payment.payment-requested.dlt.v1}
  PAYMENT_CONSUMER_GROUP: ${PAYMENT_CONSUMER_GROUP:-payment-service-v1}
  PAYMENT_CONSUMER_ENABLED: ${PAYMENT_CONSUMER_ENABLED:-false}
  PAYMENT_OUTBOX_PUBLISHER_ENABLED: ${PAYMENT_OUTBOX_PUBLISHER_ENABLED:-false}
  PAYMENT_KAFKA_RETRY_DELAYS: ${PAYMENT_KAFKA_RETRY_DELAYS:-1s,3s,10s}
  PAYMENT_OUTBOX_POLL_INTERVAL: ${PAYMENT_OUTBOX_POLL_INTERVAL:-500ms}
  PAYMENT_OUTBOX_BATCH_SIZE: ${PAYMENT_OUTBOX_BATCH_SIZE:-100}
  PAYMENT_OUTBOX_CLAIM_LEASE: ${PAYMENT_OUTBOX_CLAIM_LEASE:-30s}
  PAYMENT_OUTBOX_RETRY_BACKOFF_CAP: ${PAYMENT_OUTBOX_RETRY_BACKOFF_CAP:-60s}
  PAYMENT_RECOVERY_ENABLED: ${PAYMENT_RECOVERY_ENABLED:-false}
  PAYMENT_RECOVERY_POLL_INTERVAL: ${PAYMENT_RECOVERY_POLL_INTERVAL:-1s}
  PAYMENT_RECOVERY_BATCH_SIZE: ${PAYMENT_RECOVERY_BATCH_SIZE:-100}
  PAYMENT_RECOVERY_CLAIM_LEASE: ${PAYMENT_RECOVERY_CLAIM_LEASE:-30s}
  PAYMENT_RECOVERY_MAX_ATTEMPTS: ${PAYMENT_RECOVERY_MAX_ATTEMPTS:-10}
  PAYMENT_RECOVERY_RETRY_BACKOFF: ${PAYMENT_RECOVERY_RETRY_BACKOFF:-1s,3s,10s,30s,60s}
  PAYMENT_RECOVERY_SAFE_REPLAY_WINDOW: ${PAYMENT_RECOVERY_SAFE_REPLAY_WINDOW:-23h}

services:
  postgres:
    image: ${POSTGRES_IMAGE:-postgres:17-alpine}
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-flashsale}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      POSTGRES_DB: ${POSTGRES_DB:-flashsale_admin}
    ports:
      - "${POSTGRES_BIND_ADDRESS:-127.0.0.1}:${POSTGRES_HOST_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      <<: *platform-health-defaults
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
    networks:
      - flash-sale-net

  redis:
    image: ${REDIS_IMAGE:-redis:7.4-alpine}
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD must be set}
    ports:
      - "${REDIS_BIND_ADDRESS:-127.0.0.1}:${REDIS_HOST_PORT:-6379}:6379"
    command: ["sh", "-c", "exec redis-server --appendonly yes --requirepass \"$${REDIS_PASSWORD}\""]
    volumes:
      - redis-data:/data
    healthcheck:
      <<: *platform-health-defaults
      test: ["CMD-SHELL", "redis-cli -a \"$${REDIS_PASSWORD}\" ping | grep -q PONG"]
    networks:
      - flash-sale-net

  kafka:
    image: ${KAFKA_IMAGE:-apache/kafka:4.0.0}
    hostname: kafka
    ports:
      - "${KAFKA_BIND_ADDRESS:-127.0.0.1}:${KAFKA_HOST_PORT:-29092}:29092"
    environment:
      KAFKA_NODE_ID: "1"
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093,PLAINTEXT_HOST://:29092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:${KAFKA_HOST_PORT:-29092}
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: "1"
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: "1"
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: "1"
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: "0"
    volumes:
      - kafka-data:/tmp/kraft-combined-logs
    healthcheck:
      <<: *platform-health-defaults
      # Kafka's shell probe starts a JVM and can take more than five seconds on
      # Docker Desktop even when the broker is responsive. Keep the shared
      # interval/retry policy, but allow this heavier probe to finish.
      timeout: 20s
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --list >/dev/null 2>&1"]
    networks:
      - flash-sale-net

  schema-registry:
    image: ${SCHEMA_REGISTRY_IMAGE:-confluentinc/cp-schema-registry:8.3.0}
    hostname: schema-registry
    depends_on:
      kafka:
        condition: service_healthy
    ports:
      - "${SCHEMA_REGISTRY_BIND_ADDRESS:-127.0.0.1}:${SCHEMA_REGISTRY_HOST_PORT:-8081}:8081"
    environment:
      SCHEMA_REGISTRY_HOST_NAME: schema-registry
      SCHEMA_REGISTRY_LISTENERS: http://0.0.0.0:8081
      SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS: PLAINTEXT://kafka:9092
      SCHEMA_REGISTRY_KAFKASTORE_TOPIC: _schemas
      SCHEMA_REGISTRY_KAFKASTORE_TOPIC_REPLICATION_FACTOR: "1"
    healthcheck:
      <<: *platform-health-defaults
      test: ["CMD-SHELL", "bash -ec 'exec 3<>/dev/tcp/127.0.0.1/8081; printf \"GET /subjects HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n\" >&3; grep -q \"HTTP/1.1 200\" <&3'"]
    networks:
      - flash-sale-net

  kafka-ui:
    profiles:
      - tools
    image: ${KAFKA_UI_IMAGE:-ghcr.io/kafbat/kafka-ui:v1.5.0}
    depends_on:
      kafka:
        condition: service_healthy
      schema-registry:
        condition: service_healthy
    ports:
      - "${KAFKA_UI_BIND_ADDRESS:-127.0.0.1}:${KAFKA_UI_HOST_PORT:-8088}:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
      KAFKA_CLUSTERS_0_SCHEMAREGISTRY: http://schema-registry:8081
      DYNAMIC_CONFIG_ENABLED: "false"
    networks:
      - flash-sale-net

  api-gateway:
    <<: *app-common
    depends_on: {}
    image: ${API_GATEWAY_IMAGE:-flash-sale/api-gateway:local}
    build:
      context: ../..
      dockerfile: services/api-gateway/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-docker}
      SERVER_PORT: "8080"
      JAVA_TOOL_OPTIONS: ${JAVA_TOOL_OPTIONS:--XX:MaxRAMPercentage=75.0}
      API_DOCS_ENABLED: ${API_DOCS_ENABLED:-false}
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: "6379"
      SPRING_DATA_REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD must be set}
      GATEWAY_RATE_LIMIT_ENABLED: ${GATEWAY_RATE_LIMIT_ENABLED:-false}
      RATE_LIMIT_ENVIRONMENT: ${RATE_LIMIT_ENVIRONMENT:-docker}
      RATE_LIMIT_KEY_HMAC_SECRET: ${RATE_LIMIT_KEY_HMAC_SECRET:-}
      AUTHENTICATION_SERVICE_URL: ${AUTHENTICATION_SERVICE_URL:-http://authentication-service:8080}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI:-http://authentication-service:8080}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-flash-sale-api}
      JWT_JWK_SET_URI: ${JWT_JWK_SET_URI:-http://authentication-service:8080/.well-known/jwks.json}
      AUTH_TRUSTED_ORIGINS: ${AUTH_TRUSTED_ORIGINS:-http://localhost:3000}
      GATEWAY_CORS_ALLOWED_ORIGINS: ${AUTH_TRUSTED_ORIGINS:-http://localhost:3000}
    ports:
      - "${GATEWAY_BIND_ADDRESS:-127.0.0.1}:${GATEWAY_PORT:-8080}:8080"

  authentication-service:
    <<: *backend-common
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    image: ${AUTHENTICATION_SERVICE_IMAGE:-flash-sale/authentication-service:local}
    build:
      context: ../..
      dockerfile: services/authentication-service/Dockerfile
    environment:
      <<: *backend-environment
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/auth_db
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-flashsale}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: "6379"
      SPRING_DATA_REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD must be set}
      SPRING_LIQUIBASE_ENABLED: ${AUTH_LIQUIBASE_ENABLED:-false}
      JWT_ISSUER: ${JWT_ISSUER:-http://authentication-service:8080}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-flash-sale-api}
      JWT_KEY_ID: ${JWT_KEY_ID:-local-dev-1}
      JWT_PUBLIC_KEY_PEM: ${JWT_PUBLIC_KEY_PEM:-}
      JWT_PUBLIC_KEY_LOCATION: ${AUTH_JWT_PUBLIC_KEY_LOCATION:-file:/run/secrets/auth-jwt/jwt-public.pem}
      JWT_PRIVATE_KEY_LOCATION: ${AUTH_JWT_PRIVATE_KEY_LOCATION:-file:/run/secrets/auth-jwt/jwt-private.pem}
      AUTH_THROTTLE_HMAC_SECRET: ${AUTH_THROTTLE_HMAC_SECRET:?AUTH_THROTTLE_HMAC_SECRET must be set}
      AUTH_TRUSTED_ORIGINS: ${AUTH_TRUSTED_ORIGINS:-http://localhost:3000}
      # Authentication provisions the fixed machine clients from the same
      # runtime-only secret injected into their calling service.
      CAMPAIGN_CLIENT_ID: ${CAMPAIGN_CLIENT_ID:-campaign-service}
      CAMPAIGN_CLIENT_SECRET: ${CAMPAIGN_CLIENT_SECRET:-}
      FLASHSALE_CLIENT_ID: ${FLASHSALE_CLIENT_ID:-flashsale-service}
      FLASHSALE_CLIENT_SECRET: ${FLASHSALE_CLIENT_SECRET:-}
    volumes:
      - ${AUTH_JWT_KEY_DIR:?AUTH_JWT_KEY_DIR must point to a local key directory}:/run/secrets/auth-jwt:ro

  product-service:
    <<: *backend-common
    image: ${PRODUCT_SERVICE_IMAGE:-flash-sale/product-service:local}
    build:
      context: ../..
      dockerfile: services/product-service/Dockerfile
    environment:
      <<: *backend-environment
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/product_db
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-flashsale}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      SPRING_LIQUIBASE_ENABLED: ${PRODUCT_LIQUIBASE_ENABLED:-false}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI:-http://authentication-service:8080}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-flash-sale-api}
      JWT_JWK_SET_URI: ${JWT_JWK_SET_URI:-http://authentication-service:8080/.well-known/jwks.json}

  campaign-service:
    <<: *backend-common
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
      schema-registry:
        condition: service_healthy
    image: ${CAMPAIGN_SERVICE_IMAGE:-flash-sale/campaign-service:local}
    build:
      context: ../..
      dockerfile: services/campaign-service/Dockerfile
    environment:
      <<: *backend-environment
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/campaign_db
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-flashsale}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      SPRING_LIQUIBASE_ENABLED: ${CAMPAIGN_LIQUIBASE_ENABLED:-false}
      SPRING_KAFKA_BOOTSTRAP_SERVERS: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:-kafka:9092}
      SCHEMA_REGISTRY_URL: ${SCHEMA_REGISTRY_URL:-http://schema-registry:8081}
      KAFKA_AUTO_REGISTER_SCHEMAS: ${KAFKA_AUTO_REGISTER_SCHEMAS:-false}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI:-http://authentication-service:8080}
      JWT_ISSUER: ${JWT_ISSUER:-http://authentication-service:8080}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-flash-sale-api}
      JWT_JWK_SET_URI: ${JWT_JWK_SET_URI:-http://authentication-service:8080/.well-known/jwks.json}
      INTERNAL_JWT_AUDIENCE: ${INTERNAL_JWT_AUDIENCE:-flash-sale-internal-api}
      OAUTH_TOKEN_URI: ${OAUTH_TOKEN_URI:-http://authentication-service:8080/oauth2/token}
      CAMPAIGN_CLIENT_ID: ${CAMPAIGN_CLIENT_ID:-campaign-service}
      CAMPAIGN_CLIENT_SECRET: ${CAMPAIGN_CLIENT_SECRET:?CAMPAIGN_CLIENT_SECRET must be set}
      PRODUCT_SERVICE_URL: ${PRODUCT_SERVICE_URL:-http://product-service:8080}
      INVENTORY_SERVICE_URL: ${INVENTORY_SERVICE_URL:-http://inventory-service:8080}

  flashsale-service:
    <<: *backend-common
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
      schema-registry:
        condition: service_healthy
    image: ${FLASHSALE_SERVICE_IMAGE:-flash-sale/flashsale-service:local}
    build:
      context: ../..
      dockerfile: services/flashsale-service/Dockerfile
    environment:
      <<: *flashsale-environment

  # One-off, non-web Liquibase process. Start it explicitly with the migrations profile before
  # starting Flash Sale replicas; its completed changeset is recorded in flashsale_db.
  flashsale-migration:
    profiles:
      - migrations
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - flash-sale-net
    image: ${FLASHSALE_SERVICE_IMAGE:-flash-sale/flashsale-service:local}
    build:
      context: ../..
      dockerfile: services/flashsale-service/Dockerfile
    environment:
      <<: *flashsale-environment
      SPRING_LIQUIBASE_ENABLED: "true"
      SPRING_MAIN_KEEP_ALIVE: "false"
      SPRING_KAFKA_LISTENER_AUTO_STARTUP: "false"
      SPRING_TASK_SCHEDULING_ENABLED: "false"
      FLASHSALE_RUNTIME_ENABLED: "false"
    command: ["--spring.main.web-application-type=none"]

  order-service:
    <<: *backend-common
    depends_on:
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      schema-registry:
        condition: service_healthy
    image: ${ORDER_SERVICE_IMAGE:-flash-sale/order-service:local}
    build:
      context: ../..
      dockerfile: services/order-service/Dockerfile
    environment:
      <<: *order-environment

  # One-off, non-web Liquibase process for the Order-owned schema.
  order-migration:
    profiles:
      - migrations
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - flash-sale-net
    image: ${ORDER_SERVICE_IMAGE:-flash-sale/order-service:local}
    build:
      context: ../..
      dockerfile: services/order-service/Dockerfile
    environment:
      <<: *order-environment
      SPRING_LIQUIBASE_ENABLED: "true"
      SPRING_MAIN_KEEP_ALIVE: "false"
      ORDER_ACCEPTED_PURCHASE_CONSUMER_ENABLED: "false"
      ORDER_OUTBOX_PUBLISHER_ENABLED: "false"
      ORDER_CREATION_ENABLED: "false"
      ORDER_QUERY_ENABLED: "false"
    command: ["--spring.main.web-application-type=none"]

  payment-service:
    <<: *backend-common
    depends_on:
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      schema-registry:
        condition: service_healthy
    image: ${PAYMENT_SERVICE_IMAGE:-flash-sale/payment-service:local}
    build:
      context: ../..
      dockerfile: services/payment-service/Dockerfile
    environment:
      <<: *payment-environment

  # One-off, non-web Liquibase process for the Payment-owned schema.
  payment-migration:
    profiles:
      - migrations
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - flash-sale-net
    image: ${PAYMENT_SERVICE_IMAGE:-flash-sale/payment-service:local}
    build:
      context: ../..
      dockerfile: services/payment-service/Dockerfile
    environment:
      <<: *payment-environment
      SPRING_LIQUIBASE_ENABLED: "true"
      SPRING_MAIN_KEEP_ALIVE: "false"
      SPRING_KAFKA_LISTENER_AUTO_STARTUP: "false"
      PAYMENT_ACCEPTANCE_ENABLED: "false"
      PAYMENT_CHECKOUT_ENABLED: "false"
      PAYMENT_WEBHOOK_PROCESSING_ENABLED: "false"
      PAYMENT_CONSUMER_ENABLED: "false"
      PAYMENT_OUTBOX_PUBLISHER_ENABLED: "false"
      PAYMENT_RECOVERY_ENABLED: "false"
    command: ["--spring.main.web-application-type=none"]

  notification-service:
    <<: *backend-common
    image: ${NOTIFICATION_SERVICE_IMAGE:-flash-sale/notification-service:local}
    build:
      context: ../..
      dockerfile: services/notification-service/Dockerfile

  inventory-service:
    <<: *backend-common
    depends_on:
      postgres:
        condition: service_healthy
    image: ${INVENTORY_SERVICE_IMAGE:-flash-sale/inventory-service:local}
    build:
      context: ../..
      dockerfile: services/inventory-service/Dockerfile
    environment:
      <<: *backend-environment
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/inventory_db
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-flashsale}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      SPRING_LIQUIBASE_ENABLED: ${INVENTORY_LIQUIBASE_ENABLED:-false}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI:-http://authentication-service:8080}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-flash-sale-api}
      JWT_JWK_SET_URI: ${JWT_JWK_SET_URI:-http://authentication-service:8080/.well-known/jwks.json}

  cart-service:
    <<: *backend-common
    image: ${CART_SERVICE_IMAGE:-flash-sale/cart-service:local}
    build:
      context: ../..
      dockerfile: services/cart-service/Dockerfile

networks:
  flash-sale-net:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  kafka-data:
