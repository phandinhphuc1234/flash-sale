# Copy this file to terraform.tfvars and replace the documentation-only address with your current
# public IPv4 address. terraform.tfvars is ignored and must not be committed.
cluster_endpoint_public_access_cidrs = ["203.0.113.10/32"]
