package com.philia.flashsale.campaign.campaign.application.query;

import java.util.UUID;

/** Query for the administrative detail of one Campaign. */
public record GetCampaignDetailQuery(UUID campaignId) {
}
